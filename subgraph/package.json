{
  "name": "redpacket-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for RedPacket contract",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 redpacket",
    "create-local": "graph create --node http://localhost:8020/ redpacket",
    "remove-local": "graph remove --node http://localhost:8020/ redpacket"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
